"""FORGE — CLI-first agentic harness for frontier/local model orchestration.

FORGE orchestrates both local and frontier LLMs to build software. It is not
a coding agent — it is the environment, constraints, feedback loops, and
orchestration logic that turn models into a functioning engineering team.

Core contribution: **boundary measurement** — empirically tracking where local
models suffice and where frontier judgment is required, per task type, over time.
"""

__version__ = "0.2.0"
__all__ = ["__version__"]
